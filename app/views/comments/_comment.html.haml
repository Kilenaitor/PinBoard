= div_for comment do 
    -if not comment.user_id.nil?
        %p
            %strong
                - @username = User.find(comment.user_id).username
                = link_to @username, user_path(@username)
            = time_ago_in_words(comment.created_at)
            ago
            %br
            = comment.body  